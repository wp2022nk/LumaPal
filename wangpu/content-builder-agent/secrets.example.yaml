# 复制本文件为 secrets.local.yaml，并填写真实 API key。
# secrets.local.yaml 已加入 .gitignore，不应提交到版本库。

qwen:
  api_key: "填入 Qwen 兼容模式 API key，用于主智能体"

dashscope:
  api_key: "填入 DashScope API key，用于 Qwen TTS；可留空以复用 qwen.api_key"

tavily:
  api_key: "可选：填入 Tavily API key，用于联网搜索工具"
